import { CrmPlaceholderPage } from "@/app/crm/_components/CrmPlaceholderPage";

export default function CrmTasksPage() {
  return (
    <CrmPlaceholderPage
      description="Producer task follow-up stays lightweight here so the shell is ready for action lists without dead links."
      title="Tasks"
    />
  );
}
